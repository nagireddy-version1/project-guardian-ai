# Prompt 2 — Requirement Validation

Compare a new requirement against existing project decisions.

Identify one of:

- Conflict
- Duplicate
- Decision Drift
- No Conflict

Return structured JSON with status, severity, confidence, reason, and recommendation.

Runtime copy: `backend/app/prompts/validation.py`
